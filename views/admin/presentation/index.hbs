<div id="content" class="content">
			<!-- begin breadcrumb -->
	<ol class="breadcrumb pull-right">
		<li><a href="/admin/dashboard">Dashboard</a></li>
        <li><a href="/admin/events">Events</a></li>
		<li class="active">Presentation</li>
	</ol>
	
	<div class="box-footer">
      <a href="/admin/events/{{event_id}}/presentations/add"><button type="button" class="btn btn-success m-r-5 m-b-5">Create Presentation</button></a>
    </div>
    <br>

	<!-- begin row -->
	<div class="row">
        <!-- begin col-12 -->
        <div class="col-md-12">
            <!-- begin panel -->
            <div class="panel panel-inverse">
                <div class="panel-heading">
                    <h4 class="panel-title">Presentation Listing</h4>
                </div>
                <div class="panel-body">
                    {{#if message}}
                        <div class="alert alert-success" id="success-alert">
                            <button type="button" class="close" data-dismiss="alert">x</button>
                            <strong>Success! </strong>
                            {{message}}
                        </div>
          
                    {{/if}}
                    <table id="data-table" class="table table-striped table-bordered">
                        <thead>
                            <tr>
                                <th>Sr. No</th>
                                <th>Event Name</th>
                                <th>Question</th>
                                <th>Answer</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each presentation}}
                            <tr class="odd gradeX">
                                <td>{{inc @index}}</td>
                                <td>{{this.presentation_docs.0.event_name}}</td>
                                <td>{{this.question_name}}</td>
                                <td>{{this.answer_name}}</td>
                                
                                <td>
                                    <a title="Edit" href="/admin/events/{{event_id}}/presentations/edit/{{this._id}}" class="btn btn-primary btn-sm m-r-5"><i class="fa fa-pencil"></i></a>

                                    <a title="Delete" href="/admin/events/{{event_id}}/presentations/delete/{{this._id}}" onclick="return confirm('Do you really want to delete the current record ?');" class="btn btn-danger btn-sm m-r-5"><i class="fa fa-trash"></i></a>
                                </td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- end panel -->
        </div>
        <!-- end col-12 -->
    </div>
    <!-- end row -->
</div>